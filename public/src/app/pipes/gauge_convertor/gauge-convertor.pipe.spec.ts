import { GaugeConvertorPipe } from './gauge-convertor.pipe';

describe('GaugeConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new GaugeConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
